import React from 'react';

import { Card, CardBody } from '../../../../UI/Card/Card';

export const UserObs = props => {
    return(
        <Card>
            <CardBody>
                
            </CardBody>
        </Card>
    )
}